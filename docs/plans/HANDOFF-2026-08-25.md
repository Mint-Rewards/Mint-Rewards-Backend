# Handoff — Location Capture, backend repo (2026-08-25)

> **STATUS UPDATE 2026-09-04 — this work landed. Historical record.**
>
> The branch described below was merged: every file it lists is on `main` today
> (`lib/locationRegistry.ts`, `lib/evaluateLocation.ts`, `lib/pickupSnapshot.ts`,
> `lib/data/locationRegistry.json`, `app/api/users/location/route.ts`,
> `app/api/location/reverse-geocode/route.ts`, and their six test suites).
>
> **P3.1 was NOT lost.** `scripts/location-backfill-audit.js` and
> `__tests__/locationBackfillAudit.test.ts` are on `main`. Its key finding:
> `centroids.json` never existed in either repo or its git history, so the
> agree/disagree buckets are unreachable until centroids are supplied via
> `--centroids`.
>
> **Still open** (carried into `docs/HANDOFF.md` §6): `LOCATIONIQ_API_KEY` is
> unset in every environment; the LocationIQ attribution link is still owed in
> the app; the production backfill-audit run needs prod read credentials; and
> the three decisions parked before `LOCATION_GATE_MODE=hard` (coordinate
> plausibility bound, PATCH rate limit, global LocationIQ call cap) are
> undecided. **The cross-repo registry sync rule below is still fully in force.**

Branch: `feature/location-capture-p0`, HEAD `64dcf7a`. **Not pushed.** All work committed;
tests 235/235, `tsc --noEmit` clean.

## Completed on this branch (newest first)

| Commit    | What                                                                                                                                                                                               |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `64dcf7a` | P1 executable plan doc                                                                                                                                                                             |
| `925677e` | Final-review fix wave: affix-tolerant resolver pass, fold-tolerant city lookup, artifact structural-invariant tests, structuredAddress mutual-exclusion clears, post-bump currentVersion, 5 minors |
| `aefc657` | P1.1 `POST /api/location/reverse-geocode` + permanent geocodeCache (no TTL — deliberate)                                                                                                           |
| `a3fcd98` | P1.4 `PATCH /api/users/location` progressive save (dotted $set, legacy dual-writes, once-only completion stamp)                                                                                    |
| `248fb8b` | P1.2 `evaluateLocation` — versioned server completion truth (LOCATION_COMPLETION_VERSION = 1)                                                                                                      |
| `6846dd1` | P1.3 `locationGate` on `/api/app-config` + 5 `LOCATION_GATE_*` env vars (deployed default MUST stay `soft`)                                                                                        |
| `d50c844` | COARSE_ADMIN_UNITS guard in the backend resolver (fix round)                                                                                                                                       |
| `8ff4386` | Registry artifact `lib/data/locationRegistry.json` + `lib/locationRegistry.ts` resolver                                                                                                            |
| `a9e087a` | P0.4a pickup address snapshot: `lib/pickupSnapshot.ts` — **every future pickup writer must call `buildPickupAddressSnapshot`**; no pickup writer exists in any repo yet                            |

## In flight when this session ends

- **P3.1 backfill-audit script** — an agent was building `scripts/location-backfill-audit`
  per `docs/plans/p3-1-backfill-audit.md` (read-only; centroid-distance buckets + sixth
  `no_centroid` bucket; folds in the P0.5 deprecated-sub-area usage count and the P0.4b
  `pickupHistory` gate count; test-DB rehearsal only). If its commit is absent from
  `git log`, the work is lost — re-dispatch from that plan file. Its SDD ledger:
  `.superpowers/sdd/p3-1-backfill-audit/progress.md`.

## Not started / owner decisions

- **Production run of P3.1** — owner's call; needs prod read credentials (never available
  so far). The script's `--target=production` path is gated and read-only.
- **Parked from final review:** coordinate-plausibility bound ([0,0] + `source: map_pin`
  counts as a pin — decide before `LOCATION_GATE_MODE=hard`); rate limit on the PATCH
  endpoint; global LocationIQ call cap.
- `LOCATIONIQ_API_KEY` is not set in any environment yet — endpoint returns
  `{ resolved:false }` until it is. LocationIQ attribution link is owed in the app (free-tier condition).
- Deploy ordering: this backend (gate fields, default `soft`) deploys BEFORE app 2.1.10
  reaches the store.

## Cross-repo sync rule (load-bearing)

Any change to the app repo's `utils/pakistan_areas.ts` requires regenerating BOTH
artifacts: the app fixture (`utils/__generated__/locationRegistry.json`, tested in-repo)
and this repo's `lib/data/locationRegistry.json`. Nothing automated catches a stale
backend copy — only the documented discipline.

## Resume protocol

SDD ledgers under `.superpowers/sdd/*/progress.md` hold per-task status and every ruling
(P1's workspace was deleted after clean completion — its record is the git history and
the plan doc). Master context: `docs/plans/p1-backend-services.md`,
`docs/plans/p3-1-backfill-audit.md`, and the app repo's `docs/plans/p2-capture-flow.md`.
